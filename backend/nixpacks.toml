[phases.setup]
nixPkgs = ['python312']

[phases.install]
cmds = ['pip install -r requirements-railway.txt']

[phases.build]
cmds = []

[start]
cmd = 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'
