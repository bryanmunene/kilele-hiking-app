streamlit
requests
folium
streamlit-folium
plotly
pandas
openpyxl
sqlalchemy>=2.0.0
bcrypt>=4.0.0
pyotp
qrcode
pillow
gpxpy==1.6.2
fitparse==1.2.0
stravalib==1.6.0

# Database drivers (PostgreSQL - optional, only needed if using PostgreSQL)
# psycopg2-binary==2.9.9  # Commented out - enable when PostgreSQL credentials are added

# Image storage (Cloudinary - optional, only needed for cloud image hosting)
# cloudinary==1.41.0  # Commented out - enable when Cloudinary credentials are added

# Environment & config
python-dotenv==1.0.1

# Error tracking (Sentry - optional, only needed for error monitoring)
# sentry-sdk==1.40.0  # Commented out - enable when Sentry DSN is configured

# Email (SendGrid - optional, only needed for email notifications)
# sendgrid==6.11.0  # Commented out - enable when SendGrid API key is added
